import { EmailMatchValidator } from './email-match-validator';

describe('EmailMatchValidator', () => {
  it('should create an instance', () => {
    expect(new EmailMatchValidator()).toBeTruthy();
  });
});
